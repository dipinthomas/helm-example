apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app_z.fullname" . }}-test-connection"
  labels:
{{ include "app_z.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "app_z.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
